content : --

submit new changes to github:
    git add:
        put changes to a staging agent
    git commit -m "--":
        put changes to local computer
    git push:
        push to github


git show --(version #):
    show past modifications
git branch:
    show branches
git checkout -b (--):
    create a new branch
git checkout (--):
    be in the branch and see new changes
git log:
    show history changes
git diff:
    to show what's changed specifically
git status:
    to show what's changed in a file basis